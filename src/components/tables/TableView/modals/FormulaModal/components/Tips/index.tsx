// External Libraries
import type React from 'react'

// Components

// Styles
import { Container } from './styles'

interface Props {
  formula: string
}

export const Tips: React.FC<Props> = ({ formula }) => {
  return <Container>{}</Container>
}
